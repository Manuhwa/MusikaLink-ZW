# Quickstart

```bash
npm install
npm run dev
```

- Landing: http://localhost:3000  
- USSD: http://localhost:3000/ussd-demo  
- Agent: http://localhost:3000/agent-dashboard  

Build check: `npm run build` (must exit 0).
